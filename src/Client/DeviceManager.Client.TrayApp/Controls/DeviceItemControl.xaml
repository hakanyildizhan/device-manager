<MenuItem x:Class="DeviceManager.Client.TrayApp.DeviceItemControl"
          xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
          xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
          xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
          xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
          xmlns:local="clr-namespace:DeviceManager.Client.TrayApp"
          xmlns:vm="clr-namespace:DeviceManager.Client.TrayApp.ViewModel"
          mc:Ignorable="d" 
          Header="{Binding Name, FallbackValue=3RW55 V2.1}"
          ToolTip="{Binding Tooltip}" 
          Style="{DynamicResource MenuItemControlTemplate}"
          >

    <d:DesignerProperties.DesignStyle>
        <Style TargetType="MenuItem">
            <Setter Property="Background" 
                    Value="{x:Static SystemColors.MenuBrush}"/>
            <Setter Property="Foreground" 
                    Value="{x:Static SystemColors.MenuTextBrush}"/>
        </Style>
    </d:DesignerProperties.DesignStyle>

</MenuItem>
